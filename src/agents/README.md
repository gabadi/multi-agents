# Standalone agents

This directory is reserved for standalone agents that may run outside the main `pi.dev` TUI while still registering in the shared Fabric runtime.

Examples of future or optional standalone agents include:

- Telegram gateway agents
- external review workers
- long-running background services

The current public release focuses on the core extension, launcher, monitor, project-management domain, and skill-driven roles under `skills/`.
